<h1>Forecast for <%= @location_label %></h1>
<% if @cached %>
  <p style="color:green;">(Result pulled from cache)</p>
<% end %>

<h2>Current</h2>
<p>Temp: <%= @forecast[:current][:temp] %> °C</p>
<p>Feels like: <%= @forecast[:current][:feels_like] %> °C</p>
<p><%= @forecast[:current][:desc] %></p>

<h2>Today</h2>
<p>High: <%= @forecast[:today][:max] %> °C</p>
<p>Low: <%= @forecast[:today][:min] %> °C</p>

<h2>Next days</h2>
<ul>
  <% @forecast[:daily].first(5).each_with_index do |d,i| %>
    <li>Day <%= i+1 %> — <%= d[:desc] %>, high: <%= d[:max] %>, low: <%= d[:min] %></li>
  <% end %>
</ul>

<%= link_to "New search", root_path %>
